/**
 * An enum that represents socket.io events
 */
export enum TodoEvents {
    JOINED_ROOM = 'joined_room',
    LEFT_ROOM = 'left_room',
    ADDED = 'added',
    UPDATED = 'updated',
    DELETED = 'deleted'
}
